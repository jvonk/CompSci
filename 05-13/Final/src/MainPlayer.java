import java.awt.*;

public class MainPlayer extends Player {
    public MainPlayer(int x, int y) {
        super(x, y);
    }

    @Override
    public void drawMe(Graphics2D g) {
        super.drawMe(g);
    }
}
